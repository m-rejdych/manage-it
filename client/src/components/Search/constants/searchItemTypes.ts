import { SearchItemType } from '../types/SearchItem';

const SEARCH_ITEM_TYPES: Record<string, SearchItemType> = {
  USER: 'user',
  PROJECT: 'project',
  TASK: 'task',
};

export default SEARCH_ITEM_TYPES;
